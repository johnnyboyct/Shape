/*
 * Ellipse Messages
 *
 * This contains all the text for the Ellipse component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Ellipse';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Ellipse component!',
  },
});
